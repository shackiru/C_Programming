#include <stdio.h>
#include <string.h>
#include <stdlib.h>

struct Node
{
    char name[100];
    int age;
    Node *next, *prev;
};

int main()
{
    Node *head = NULL, *tail = NULL;

    return 0;
}