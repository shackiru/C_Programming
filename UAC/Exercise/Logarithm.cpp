#include <stdio.h>

int main()
{
    int testCase;
    int count = 0;
    long long int num;
    long long int maxNum;

    scanf("%d", &testCase);
    for(int tc = 0; tc < testCase; tc++)
    {
        scanf("%d %d", &num, &maxNum);
        if()
        {
            
        }
        printf("Case#%d: %d\n", tc + 1, count);
    }

    return 0;
}